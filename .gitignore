# Rust / Cargo
/target/
**/*.rs.bk
Cargo.lock.bak

# Build artifacts
*.o
*.a
*.so
*.dylib
*.dll

# Secrets, credentials & local config
.env
.env.*
!.env.example
config.local.toml
*.local.toml
*.local.yml
*.local.json

# Key / wallet material (never commit)
*.pem
*.p12
*.pfx
*.key
*.keystore
*.mnemonic
id_rsa*
**/wallet.json
*secret*
*secrets*
credentials/
keystore/
wallet/
PRIVATE_KEY
PRIVATE_KEY_FILE
**/private_key*
**/operator.key
*.mnemonic.txt

# Local oracle-feed proposal scratch files
proposed-feeds.txt
proposed-feeds*.txt

# Foundry / Huff artifacts if ever copied into this tree
/out/
/cache/
/broadcast/
/artifacts/

# External integrations (certs, registration keys)
/external_gateway/

# Runtime data & journals
sentinel/state.json
.rpbot-*
.debug-journal.md
opportunities.jsonl
*.jsonl

# Profiling & benchmarks
flamegraph*.svg
perf.data
perf.data.old
*.profraw
/target/criterion/

# Code intelligence / doc generators (regenerable)
docs/
graphify-rs-out/
graphify-out/
graphify-out-routing/

# AI / agent tooling (not functional workspace)
.agents/
.claude/
.codex/
.codegraph
.cursor/
.grok/
.omo/
.serena/
.symlens/

# IDE & OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Logs (may contain RPC URLs, addresses, session metadata)
*.log
target/run-logs/
run-logs/