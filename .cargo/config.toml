# Host-local build flags for this machine (i5-9500). Do not copy to other hosts
# without revisiting target-cpu / linker paths.

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = [
    "-Zthreads=2",
    "-Ctarget-cpu=native",
    "-Clink-arg=--ld-path=/usr/local/bin/mold",
]
